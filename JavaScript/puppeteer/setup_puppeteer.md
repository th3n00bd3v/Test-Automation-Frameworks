## Puppeteer - JavaScript

Puppeteer is a JavaScript library which provides a high-level API to control browsers over the DevTools Protocol or WebDriver BiDi. 
It runs in the headless (no visible UI) by default but can be configured to run in a visible ("headful") browser.